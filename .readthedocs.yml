version: 2

build:
  apt_packages:
    - graphviz
  os: ubuntu-24.04
  tools:
    python: '3.11'

sphinx:
  configuration: docs/conf.py


python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
      extra_requirements:
        - docs

formats: []
